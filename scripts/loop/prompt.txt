You are taking a turn in an alternating agent loop on this repo.

Read AUTO_LOOP.md first and follow it exactly. Your turn id is: __TURN_ID__

In short: run `scripts/loop/turn.sh before __TURN_ID__`, LOOK at the three
frames it writes to runs/__TURN_ID__/before/, read HANDOFF.md, choose ONE
meaningful improvement justified by what you saw, build it, prove it with
`scripts/loop/turn.sh after __TURN_ID__`, commit and tag on the auto-loop
branch, then append your entry to HANDOFF.md.

Do not touch main. Do not revert the other agent's work without saying why.
